{
    "version": 2,
    "name": "connectech",
    "builds": [{
        "src": "/server",
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/server"
    }]
}
